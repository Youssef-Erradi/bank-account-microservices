server:
  port: 9091

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: bankaccountdb

security:
  oauth2:
    resource:
      jwt:
        key-value: bankSecretKey
